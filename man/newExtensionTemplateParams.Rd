% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/constructor.R
\name{newExtensionTemplateParams}
\alias{newExtensionTemplateParams}
\title{Create a MizerParams object using the extension template}
\usage{
newExtensionTemplateParams(
  species_params,
  season_amplitude = 0.2,
  extra_food_coef = 0.1,
  background_mort_coef = 0.05,
  plankton_rate = 0.5,
  ...
)
}
\arguments{
\item{species_params}{A data frame of species parameters passed directly to
\code{\link[mizer:newMultispeciesParams]{mizer::newMultispeciesParams()}}.}

\item{season_amplitude}{Amplitude of the sinusoidal seasonal variation in
the encounter rate, as a fraction of the base rate. \code{0} disables the
effect; the default \code{0.2} gives ±20 \% variation over the year.}

\item{extra_food_coef}{Coefficient for the allometric extra food source
added via \code{setExtEncounter()}. Set to \code{0} to disable.}

\item{background_mort_coef}{Coefficient for background mortality added via
\code{setExtMort()}. Set to \code{0} to disable.}

\item{plankton_rate}{Intrinsic growth rate of the plankton component
(yr⁻¹). Higher values make the plankton respond faster to depletion.}

\item{...}{Additional arguments passed to \code{\link[mizer:newMultispeciesParams]{mizer::newMultispeciesParams()}}.}
}
\value{
A \code{MizerParams} object of class \code{"mizerExtensionTemplate"}.
}
\description{
This constructor demonstrates all five extension mechanisms available in
mizer. Read the inline comments to understand what each block does and how
to adapt it for your own extension.
}
\details{
\subsection{Extension mechanisms demonstrated}{
\enumerate{
\item \strong{\code{setExtEncounter()} / \code{setExtMort()}} — add fixed, species × size
arrays to encounter or mortality without any dynamics.
\item \strong{\verb{project*} S3 method} — seasonal encounter multiplier implemented in
\code{\link[=projectEncounter.mizerExtensionTemplate]{projectEncounter.mizerExtensionTemplate()}}, replacing the
\code{setRateFunction()} approach.
\item \strong{\code{setComponent()}} — a dynamical plankton component with its own
time-evolution and an encounter contribution. Defined in
\code{component-functions.R}.
\item \strong{S3 generic overrides} — \code{\link[=getBiomass.mizerExtensionTemplate]{getBiomass.mizerExtensionTemplate()}} and
\code{\link[=getBiomass.mizerExtensionTemplateSim]{getBiomass.mizerExtensionTemplateSim()}} add the plankton biomass to the
standard output.
}
}

\subsection{Metadata-only vs. dispatching extensions}{

This constructor creates a \strong{dispatching} extension: the returned object
has class \code{"mizerExtensionTemplate"} so that mizer's generic functions
dispatch to the S3 methods defined in this package. The marker class is
\strong{not} defined statically with \code{setClass()}; mizer recognises this package
as a dispatching extension from the S3 methods it registers (see
\code{mizerExtensionTemplate-class}) and creates the class dynamically at load
time, which is what allows it to be chained with other extensions. The
\code{\link[mizer:coerceToExtensionClass]{coerceToExtensionClass()}} call at the end of this function then promotes
the object to that class. For a \strong{metadata-only} extension (one that does
not override any generic and so registers no dispatch methods), you would
omit the \code{coerceToExtensionClass()} call; you still call
\code{params@extensions <- getRegisteredExtensions()} so the dependency is
recorded.
}
}
\seealso{
\code{\link[=projectEncounter.mizerExtensionTemplate]{projectEncounter.mizerExtensionTemplate()}},
\code{\link[=planktonDynamics]{planktonDynamics()}}, \code{\link[=getBiomass.mizerExtensionTemplate]{getBiomass.mizerExtensionTemplate()}}
}
