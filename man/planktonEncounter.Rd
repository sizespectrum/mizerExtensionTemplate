% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/component-functions.R
\name{planktonEncounter}
\alias{planktonEncounter}
\title{Plankton encounter contribution}
\usage{
planktonEncounter(params, n, n_pp, n_other, component, ...)
}
\arguments{
\item{params}{A MizerParams object.}

\item{n}{Species abundance matrix (species × size).}

\item{n_pp}{Resource abundance vector (full size grid).}

\item{n_other}{Named list of other component states, including the current
plankton spectrum under \code{n_other[["plankton"]]}.}

\item{component}{Name of this component (\code{"plankton"}).}

\item{...}{Additional arguments (ignored).}
}
\value{
A numeric matrix (species × size) of extra encounter rates due to
plankton.
}
\description{
Computes the extra encounter rate that fish experience by feeding on the
plankton component. This function is registered as the \code{encounter_fun} of
the "plankton" component in \code{\link[=newExtensionTemplateParams]{newExtensionTemplateParams()}} and is called
automatically by \code{\link[mizer:getEncounter]{mizer::getEncounter()}} at each time step.
}
\details{
The implementation is taken directly from the detritus example in
\code{vignette("extending-mizer", package = "mizer")}. It treats the plankton
spectrum as the resource (\code{n_pp}) in a standard mizer encounter calculation,
after first removing the plankton's own encounter hook from the temporary
params copy to avoid double-counting.
}
