% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/component-functions.R
\name{planktonDynamics}
\alias{planktonDynamics}
\title{Plankton dynamics}
\usage{
planktonDynamics(params, n_other, rates, dt, component, ...)
}
\arguments{
\item{params}{A MizerParams object.}

\item{n_other}{Named list of current component states. The plankton
spectrum is available as \code{n_other[["plankton"]]}.}

\item{rates}{A named list of rates as returned by \code{\link[mizer:getRates]{mizer::getRates()}}.
Used to compute predation mortality on the plankton spectrum via
\code{rates$pred_rate}.}

\item{dt}{Length of the current time step (years).}

\item{component}{Name of this component (\code{"plankton"}).}

\item{...}{Additional arguments (ignored).}
}
\value{
Updated plankton abundance vector (full size grid).
}
\description{
Updates the plankton component spectrum at each time step. This function is
registered as the \code{dynamics_fun} of the "plankton" component in
\code{\link[=newExtensionTemplateParams]{newExtensionTemplateParams()}} and is called automatically by \code{\link[mizer:project]{project()}}.
}
\details{
The plankton grows towards a size-specific carrying capacity at a fixed rate
and is simultaneously depleted by predation. The update is the analytical
solution of the linear ODE:

\deqn{dP/dt = r(K - P) - m \cdot P = rK - (r + m)P}

where \code{r} is the growth rate, \code{K} is the carrying capacity, and \code{m} is the
predation mortality on the plankton spectrum at each size bin.

This implementation follows the detritus example in
\code{vignette("extending-mizer", package = "mizer")}.
}
