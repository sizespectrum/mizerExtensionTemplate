% Generated by roxygen2: do not edit by hand
% Please edit documentation in R/rate-methods.R
\name{projectEncounter.mizerExtensionTemplate}
\alias{projectEncounter.mizerExtensionTemplate}
\title{Seasonal encounter rate for the extension template}
\usage{
\method{projectEncounter}{mizerExtensionTemplate}(params, n, n_pp, n_other, t = 0, ...)
}
\arguments{
\item{params}{A MizerParams object of class \code{"mizerExtensionTemplate"}.}

\item{n}{Species abundance matrix (species × size).}

\item{n_pp}{Resource abundance vector (full size grid).}

\item{n_other}{Named list of other component states.}

\item{t}{Current simulation time (years). The seasonal multiplier is
\verb{1 + amplitude * sin(2π t)}, which peaks at \code{t = 0.25} and troughs at
\code{t = 0.75}.}

\item{...}{Additional arguments passed through the dispatch chain.}
}
\value{
A numeric matrix (species × size) of encounter rates, seasonally
scaled.
}
\description{
Applies a sinusoidal seasonal multiplier on top of the encounter rate
computed by lower extensions and the base mizer calculation.
}
\details{
This method replaces the \code{setRateFunction()} approach used in single-user
scripts. It participates in the S3 dispatch chain so that multiple loaded
extension packages can each modify the encounter rate without overwriting
one another.
\subsection{Why \verb{project*} methods instead of \code{setRateFunction()}}{

\code{setRateFunction(params, "Encounter", "myFn")} stores a single function
name per rate. If two extension packages both call it for the same rate,
the second silently overwrites the first. S3 dispatch via \verb{project*}
methods avoids this: each extension defines a method for its own marker
class and calls \code{NextMethod()} to pass control down the chain.
}

\subsection{Rules for every \verb{project*} method}{
\enumerate{
\item \strong{Always call \code{NextMethod()}} — omitting it silently drops all
contributions from lower extensions and the base mizer calculation.
\item \strong{Keep the same argument signature} as the generic and include \code{...}
so extra arguments pass through to lower methods.
\item \strong{Do not call \code{setRateFunction()}} in your constructor for any rate
handled by a \verb{project*} method in this package.
}
}
}
